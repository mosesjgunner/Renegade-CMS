# Evidence — A-09 Draft/private/admin access hardening

## 1. Identity and checkpoint

- Card: `A-09`
- Branch: `phase-a/a09`
- Base commit: `440da6600b6f3666c803b9977094b9d8c06ccc6a`
- Checkpoint rule frozen here: downstream cards may treat A-09 as satisfied only when this
  evidence file is present at an ancestor commit and the focused access test passes. Branch name
  or ancestry of the control-plane commit alone is not sufficient.

## 2. Implemented access boundary

- Payload REST/GraphQL reads of canonical `content` and `media-assets` require a staff role.
- The public renderer and `/media/[id]` byte route retain their explicit, scoped publication
  checks. Raw collection access is therefore not a substitute for public visibility.
- No schema, migration, generated type, import map, storage adapter, or public media URL changed.

## 3. Verification

| Command | Result |
| --- | --- |
| `npx vitest run tests/unit/phase-a-access.test.ts` | Passed: 2 tests |
| `npm run typecheck` | Passed |

## 4. Definition-of-done verdict

**VERIFIED for the C-7 prerequisite boundary.** A-02 must still prove its own C-8 workflow
requirements against its isolated PostgreSQL/filesystem/browser resources.
