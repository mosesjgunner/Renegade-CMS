# Evidence — A-01 Clean install, setup, and first login

> **Partial implementation only.** Required Compose, restart, browser, and log proof remains outstanding; this record does not declare A-01 ready.

## 1. Identity & commit binding

- Card: `A-01`; requested worktree: `phase-a/a01-install-login`; actual checkout: `main`.
- Base SHA: `ae0d121652d4e6507a327f68c029c0512588bcdd`.
- Final SHA described: `d1288f694dec9d06088e4950d541a816bcadbda0` plus uncommitted changes below.
- Checkpoint: A-00 is merged (`6f4d54e` is an ancestor of `HEAD`). A-09 is not merged, so Group 0 requires this work not be merged until A-09 is reconciled first.
- Assigned resources used: disposable `renegade_a01_db` on port `5433`, database `renegade_a01`; removed after the run. No default port/database/media resource was used.

## 2. Changed files

```text
src/app/(frontend)/setup/setup-form.tsx
tests/browser/phase-a-install-login.spec.ts
tests/integration/installation.integration.test.ts
docs/execution/phase-a/evidence/A-01.md
docs/execution/phase-a/remediation/A-01-verification.md
```

## 3. Migrations

- New migrations/schema changes: none.
- Payload generated types/import map: unchanged; regeneration not required.

## 4. Commands executed

| Command                                                                                                                                                       | Exit code | Notes                                                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------: | ------------------------------------------------------------------------------- |
| `npx prettier --check src/app/(frontend)/setup/setup-form.tsx tests/browser/phase-a-install-login.spec.ts tests/integration/installation.integration.test.ts` |         0 | Passed.                                                                         |
| `git diff --check`                                                                                                                                            |         0 | Passed.                                                                         |
| `npm run lint`                                                                                                                                                | no result | Runner stopped the Node process at its 30-second ceiling.                       |
| `npm run typecheck`                                                                                                                                           | no result | Same host limit.                                                                |
| `npm run build`                                                                                                                                               |   NOT RUN | Remediation required.                                                           |
| `npm run test:integration -- tests/integration/installation.integration.test.ts`                                                                              | no result | Started against A-01 port 5433; runner stopped before Vitest reported a result. |
| `npx playwright test tests/browser/phase-a-install-login.spec.ts`                                                                                             |   NOT RUN | Needs isolated started instance and token from local web-container logs.        |
| Supported clean Compose/installer smoke                                                                                                                       |   NOT RUN | Remediation required.                                                           |

## 5. Test counts

- Unit: not run.
- Integration: no completed result.
- Browser: not run.

## 6. Browser/API/manual proof

`tests/browser/phase-a-install-login.spec.ts` imports no Payload, installation service, or database client. It takes `A01_SETUP_TOKEN` only from the supported local container-log handoff, then uses the real HTTP routes plus a Chromium virtual WebAuthn authenticator for invalid/reused token assertions, visible setup, recovery-code one-time display, setup lock, logout, and clean-cookie passkey login. Trace and video are enabled. No runtime artifact is claimed.

The integration suite now asserts that an expired bootstrap token returns `INSTALLATION_EXPIRED` and leaves the installation incomplete with no registration challenge. It has not completed here.

## 7. Traces & artifacts

- No trace, video, HTTP capture, or log capture was produced.

## 8. Limitations / failed boundaries

- No completed lint/typecheck/build/integration/browser result.
- No supported Compose clean install, container restart, post-restart `/setup` HTTP proof, clean-session browser proof, or log-redaction inspection.
- The active checkout is `main`, not the requested A-01 worktree; A-09 remains unmerged.

## 9. Security effects

- Completion now documents the exact boundary: TLS terminates at Caddy, Nginx, Traefik, or equivalent; Renegade remains loopback-bound; trusted proxies replace forwarded headers. Public-site and `/admin` links remain on the completion screen.
- Canonical identity, production-origin validation, locale, indexing-safe prelaunch behavior, Payload/PostgreSQL/Next.js, passkey architecture, permanent installation lock, recovery-code storage, and Lean/Standard profiles are unchanged.
- The short-lived setup token is intentionally locally logged for operator handoff. Recovery-code and secret log inspection remains unperformed.

## 10. Generated-file effects

- `generate:types` / `generate:importmap`: No.

## 11. Remediation

- `docs/execution/phase-a/remediation/A-01-verification.md`.

## 12. Definition-of-done verdict

- Verdict: **PARTIAL**.
- Focused coverage and completion-boundary copy exist, but no supported Compose run, restart proof, clean-session browser result, trace/video, HTTP capture, or log inspection has completed. This consumes C-12/C-13 without creating a competing architecture.
