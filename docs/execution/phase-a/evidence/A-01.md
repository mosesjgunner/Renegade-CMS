# Evidence — A-01 Clean install, setup, and first login

> **Execution stopped before implementation or runtime verification.** The frozen Group 0
> merge checkpoint is not satisfied by the current repository topology; proceeding would
> violate `SHARED_CONTRACTS.md` rather than prove a supported install.

## 1. Identity & commit binding

- Card: `A-01`
- Requested branch/worktree: `phase-a/a01-install-login`
- Actual checked-out branch: `main`
- Current SHA: `8f0a85ba73c3cdc8ce793c4d9aa43054b1356b6d`
- Base SHA required by the control plane: `ae0d121652d4e6507a327f68c029c0512588bcdd`
- Required checkpoint: A-00 merged, then **A-09 merged before A-01 is rebased/merged**.
- Checkpoint result: **NOT SATISFIED**. A-00 is an ancestor of `HEAD` (`git merge-base
  --is-ancestor ae0d121... HEAD` exited `0`), but `phase-a/a09` is not an ancestor of
  `main` (`git merge-base --is-ancestor phase-a/a09 main` exited `1`). `CHECKLIST.md`
  also records A-09 as `NOT STARTED` / `Not merged`.

## 2. Repository-state finding

- `HEAD` is already the merge commit `8f0a85b` (`Merge pull request #2 from
  mosesjgunner/phase-a/a01-install-login`) on `main`, rather than the requested A-01
  worktree.
- The current A-09 ref (`phase-a/a09`) points at `ddf65fa`, a documentation commit that
  is not merged into `main`; it does not establish the required A-09 access-hardening
  checkpoint.
- `docs/execution/phase-a/evidence/A-01.md` did not exist before this record. No source,
  schema, generated artifact, test, compose, env, volume, or other documentation file was
  modified by this attempt.

## 3. Commands executed (exact command + exit code)

| Command | Exit code | Notes |
|---|---:|---|
| `git log --oneline --decorate -8` | 0 | Identified current main/A-01 merge topology. |
| `git branch --show-current` | 0 | Returned `main`, not the requested worktree branch. |
| `git merge-base --is-ancestor ae0d121652d4e6507a327f68c029c0512588bcdd HEAD` | 0 | Confirms A-00 base is reachable. |
| `git merge-base --is-ancestor phase-a/a09 main` | 1 | Confirms A-09 is not merged into main. |
| `git log --oneline main..phase-a/a09` | 0 | A-09 ref contains only unmerged control-plane documentation. |
| `npm run lint` | NOT RUN | Blocked before code/test execution by frozen merge checkpoint. |
| `npm run typecheck` | NOT RUN | Same blocker. |
| `npm run build` | NOT RUN | Same blocker. |
| `npm run test:integration -- tests/integration/installation.integration.test.ts` | NOT RUN | Same blocker; assigned A-01 Postgres resource was not started. |
| `npx playwright test tests/browser/phase-a-install-login.spec.ts` | NOT RUN | Same blocker; no A-01 browser server or artifacts were created. |
| Supported clean Compose/installer smoke path | NOT RUN | Same blocker; no A-01 volumes, media path, or compose project were created. |

## 4. Assigned runtime resources

None were used. Reserved, but intentionally untouched: Compose project `renegade_a01`,
web port `3101`, Playwright port `3201`, Postgres host port `5433`, databases
`renegade_a01` / `renegade_mig_a01`, media path `.phase-a/a01/media`, and volume
`renegade_a01_media`.

## 5. Migrations and generated files

- New migrations: none.
- Schema changes: none.
- `generate:types` / `generate:importmap` required: no.

## 6. Browser / API / manual proof

No acceptance claim is made. In particular, fresh setup, WebAuthn registration, recovery
code one-time display, setup locking after restart, logout, and a clean-session login have
not been reproduced in this run. No HTTP proof, trace, video, screenshot, or secret-log
inspection artifact was produced.

## 7. Security and contract effects

No authentication or installation code was changed. Canonical site identity, production
origin, locale, indexing-safe prelaunch behavior, Payload/PostgreSQL/Next.js architecture,
passkey architecture, permanent installation lock, recovery-code behavior, and Lean/Standard
deployment profiles therefore remain untouched.

The stop follows `SHARED_CONTRACTS.md` C-12/C-13 and its Group 0 merge checkpoint: A-01
must not create a competing or unreviewed authentication/install architecture before A-09
hardening is merged.

## 8. Limitation and bounded remediation

The user-directed documentation scope permits writing only this evidence file, so no
separate remediation file was created. Required next action: merge the actual A-09
hardening work into `main`, create/check out the intended A-01 worktree from that checkpoint,
then rerun the full A-01 installation acceptance using only the assigned resources. That
rerun must replace this incomplete record with executable browser, HTTP, restart, and log
proof.

## 9. Definition-of-done verdict

- Verdict: **INCOMPLETE**.
- Justification: The required Group 0 merge order is objectively unmet and the working
  directory is `main` after an earlier A-01 merge. Running or changing the requested
  install/login flow now would bypass the frozen contract’s A-09-first reconciliation
  boundary. No definition-of-done behavior has been claimed or verified.
