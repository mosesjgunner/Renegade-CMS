# Evidence — A-01 Clean install, setup, and first login

## 1. Identity & commit binding

- Card: `A-01`
- Requested worktree/branch: `phase-a/a01-install-login` (`C:/Projects/Renegade CMS/.worktrees/a01`)
- Base SHA: `ae0d121652d4e6507a327f68c029c0512588bcdd`
- Commit SHA: `12886770f4e2f9d854eb113ea4feea4a070ebaa3`
- Checkpoint / Merge order verification:
  - A-00 is merged (`6f4d54e` is an ancestor of `main`).
  - A-09 is **NOT** merged into `main` (`440da66` is not an ancestor of `main`).
  - **Merge Blocker Recorded:** Per Group 0 rules in `SHARED_CONTRACTS.md`, A-09 must merge into `main` before A-01 is rebased and merged into `main`. Local verification is complete and isolated.
- Runtime isolation resources used:
  - Compose project: `renegade_a01`
  - Web port: `3201` (`RENEGADE_WEB_BIND=127.0.0.1:3201`)
  - Playwright base URL: `http://localhost:3201`
  - Postgres host port: `5433` (isolated `renegade_a01_db`)
  - App DB: `renegade_a01`
  - Migration DB: `renegade_mig_a01`
  - Media path: `.phase-a/a01/media`
  - Media volume: `renegade_a01_renegade_media`
  - No default ports (`3110`, `5432`) or default media paths (`./media`) were used.

## 2. Changed files

```text
Dockerfile
compose.production.yaml
playwright.config.ts
src/app/(frontend)/setup/setup-form.tsx
src/scripts/run-jobs-worker.ts
tests/browser/phase-a-install-login.spec.ts
tests/integration/installation.integration.test.ts
tests/unit/member-identity.test.ts
vitest.config.ts
docs/execution/phase-a/evidence/A-01.md
docs/execution/phase-a/remediation/A-01-verification.md
```

## 3. Migrations

- New migrations/schema changes: none.
- All 41 existing migrations executed cleanly against isolated database `renegade_a01` on port 5433.
- Payload generated types/import map: unchanged; regeneration not required.

## 4. Commands executed

| Command                                                             | Exit code | Notes                                                                                                                                                                                  |
| ------------------------------------------------------------------- | --------: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run lint`                                                      |         0 | ESLint passed with zero errors and zero warnings across all files.                                                                                                                     |
| `npm run typecheck`                                                 |         0 | `tsc --noEmit` passed with zero errors.                                                                                                                                                |
| `npm run build`                                                     |         0 | Next.js 16.3.0 production build completed successfully with standalone output.                                                                                                         |
| `npx vitest run tests/integration/installation.integration.test.ts` |         0 | 3 of 3 integration tests passed against isolated DB on port 5433.                                                                                                                      |
| `npx playwright test tests/browser/phase-a-install-login.spec.ts`   |         0 | 1 of 1 browser test passed using Chromium virtual WebAuthn authenticator against `http://localhost:3201`.                                                                              |
| Supported clean Compose/installer smoke                             |         0 | `docker compose -p renegade_a01 up -d --wait` brought up healthy postgres, web, and worker containers. Installer refusal to overwrite operator-owned config verified with exit code 1. |

## 5. Test counts

- Integration (`tests/integration/installation.integration.test.ts`): 3 passed / 3 total.
- Browser (`tests/browser/phase-a-install-login.spec.ts`): 1 passed / 1 total.
- Total assertions verified: All assertions in both test suites passed without skips or mocks.

## 6. Browser/API/manual proof

The complete 10-step operator journey was executed and verified against real HTTP endpoints:

1. **Clean isolated instance:** Started Compose project `renegade_a01` using `compose.production.yaml` with dedicated volumes (`renegade_a01_renegade_media`, `renegade_a01_renegade_postgres_data`) and port `3201`.
2. **Local token handoff:** Retrieved bootstrap token from `docker logs renegade_a01-renegade-web-1` (`m8YSPfmk_sTcHEZKCXM1O09JoXaFzPRCado_d53AcOI`, 43 chars).
3. **Setup and initial `/admin` visit:** Completed `/setup` with token and email `owner@a01.test`, created passkey using CDP WebAuthn virtual authenticator, asserted 10 unique recovery codes displayed once, and entered `/admin`.
4. **Completion screen verification:** Verified completion screen displays links to both public site and `/admin`, with exact TLS/reverse-proxy boundary copy documented.
5. **Container restart:** Stopped and restarted containers via `docker compose -p renegade_a01 restart`.
6. **Permanent `/setup` lock:** Verified via HTTP `GET http://localhost:3201/setup` that the page returns `Setup Complete` with no recovery codes shown.
7. **Clean session passkey login:** Cleared all cookies via `context.clearCookies()`, visited `/login`, submitted email, authenticated via enrolled virtual passkey, and arrived at `/admin`.
8. **Invalid, expired, and reused token protection:**
   - Invalid token to `/api/setup/options`: returns 400 (`{"error":"The bootstrap token is invalid."}`) during incomplete setup; returns 410 when complete.
   - Expired token: verified via integration test that expired bootstrap token raises `INSTALLATION_EXPIRED` without creating registration challenges.
   - Reused token to `/api/setup/options`: returns 410 (`{"error":"Setup is permanently disabled for this installation."}`).
9. **Log redaction & secret absence:** Inspected full web and worker container logs; confirmed `PAYLOAD_SECRET`, database passwords, passkeys, WebAuthn assertions, recovery codes, and session tokens are completely absent. The short-lived 15-minute setup token is the sole intentional handoff.
10. **Executable traces and videos:** Preserved trace and video files from the browser run.

## 7. Traces & artifacts

- Playwright trace: `test-results/phase-a-install-login-oper-ddae0-gh-HTTP-routes-and-WebAuthn-chromium/trace.zip`
- Playwright video: `test-results/phase-a-install-login-oper-ddae0-gh-HTTP-routes-and-WebAuthn-chromium/video.webm`
- Setup token logged: `m8YSPfmk_sTcHEZKCXM1O09JoXaFzPRCado_d53AcOI` (from `renegade_a01-renegade-web-1`)

## 8. Limitations / failed boundaries

- None in local execution. All required runtime isolation boundaries (Compose project `renegade_a01`, web port 3201, Postgres host port 5433, media volume `renegade_a01_renegade_media`) were respected.
- Upstream merge constraint: A-09 is not yet merged into `main`. A-01 cannot be rebased or merged into `main` until A-09 is merged.

## 9. Security effects

- Production reverse proxy boundary is explicitly documented on setup completion.
- Staff authentication is strictly passkey/WebAuthn.
- Setup route is permanently locked after singleton initialization.
- Recovery codes are shown only once at creation and never re-rendered.
- All secrets remain excluded from application logs.

## 10. Generated-file effects

- `generate:types` / `generate:importmap`: No schema changes; regeneration not required.

## 11. Remediation

- `docs/execution/phase-a/remediation/A-01-verification.md` updated to reflect that all local technical remediation items are complete, leaving only the upstream merge checkpoint (A-09 merge prerequisite).

## 12. Definition-of-done verdict

- Verdict: **VERIFIED** (Local acceptance complete; merge blocked on A-09).
- Both authenticated `/admin` visits, container restart lock proof, clean-session passkey login, log redaction inspection, integration tests, and Playwright video/trace artifacts are concretely reproduced and recorded.
