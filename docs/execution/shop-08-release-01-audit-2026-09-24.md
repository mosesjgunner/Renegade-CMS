# SHOP-08 and RELEASE-01 audit — 2026-09-24

## Decision and provenance

**BLOCKED.** Base HEAD `91d6108443ecd0dfecfb60ee997b8e7e6ee55d36` does not identify this candidate: the working tree contains pre-existing modified, added, and staged-deleted files. This audit preserves them. The older `shop-08-final-release-gate-2026-09-24.md` claims all gates passed on a different SHA (`b1dde69`) but has no commerce browser specification in `tests/browser`; its commerce and nine-surface release claims are not established for this tree. The newer `shop-08-prompt-2-release-proof-2026-09-24.md` reports partial install and restore proof. No real provider, payout, charge, or POD order was exercised in this audit.

## Tested here

| Gate                                                                                                                                                                                                                              | Result    | Boundary                                                                                                                                   |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `npm.cmd run typecheck`                                                                                                                                                                                                           | Pass      | Current working tree.                                                                                                                      |
| `npm.cmd run lint`                                                                                                                                                                                                                | Pass      | Zero warnings.                                                                                                                             |
| `npm.cmd exec -- vitest run tests/unit/shop-08-command-center-boundaries.test.ts tests/unit/commerce-checkout.test.ts tests/unit/shop-04-payment-operations.test.ts tests/unit/shop-07-donations-contract.test.ts --reporter=dot` | Pass      | 4 files, 32 tests; unit contracts only.                                                                                                    |
| `npm.cmd run build`                                                                                                                                                                                                               | Blocked   | Windows `EBUSY` removing `.next/standalone`, which is held by existing Node processes. No process was stopped.                             |
| Fresh migration, clean install, integration, browser, backup/restore                                                                                                                                                              | Not rerun | Earlier partial proof covers isolated synthetic DB work; it does not identify this modified candidate or prove customer commerce journeys. |

## SHOP-00–07 capability ledger

`Working` below means an executable code path exists; it does not claim an end-to-end pass. `Partial` means the required customer/operator journey is unproven or broken. Provider dependency names the remaining external boundary.

| Capability                                              | State       | Source path                                                                                                                                                                                                            | Evidence here / dependency                                                                                                                                                                                                                  |
| ------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SHOP-00 scoped commerce model and domain contracts      | partial     | `src/modules/commerce/contracts.ts`, `src/modules/commerce/site-scope.ts`, `src/modules/commerce/payload-domain.ts`                                                                                                    | Unit contracts present; live cross-site attack unexecuted. No provider needed for scope proof.                                                                                                                                              |
| SHOP-01 catalog, variants, readiness                    | partial     | `src/modules/commerce/catalog.ts`, `src/modules/commerce/ProductView.tsx`, `src/app/(frontend)/store/page.tsx`                                                                                                         | Catalog unit tests exist; operator publication and buyer variant browser flow unexecuted. POD catalog depends on configured mapping/provider for live claims.                                                                               |
| SHOP-02 carts, merge, promotions, server quote          | partial     | `src/modules/commerce/cart.ts`, `src/modules/commerce/proposal.ts`, `src/modules/commerce/CartView.tsx`, `src/app/(frontend)/api/commerce/cart/merge/route.ts`                                                         | Checkout unit tests passed; cart race and actual guest-to-member merge unexecuted. No provider for quote proof.                                                                                                                             |
| SHOP-03 POD preflight and fulfillment                   | partial     | `src/modules/commerce/pod-operations.ts`, `src/modules/commerce/pod-reconciliation.ts`, `src/modules/commerce/pod-emulator.ts`, `src/app/(frontend)/admin/fulfillment/page.tsx`                                        | Emulator and unit paths exist; paid order to tracking through normal UI unexecuted. Live provider needs credentials; no live order authorized.                                                                                              |
| SHOP-04 hosted checkout, webhook, order, refund/dispute | partial     | `src/app/(frontend)/api/commerce/checkout/initiate/route.ts`, `src/app/(frontend)/api/commerce/webhooks/[provider]/route.ts`, `src/modules/commerce/payment-operations.ts`, `src/modules/commerce/payment-provider.ts` | Focused unit tests passed. No browser guest purchase, duplicate/out-of-order webhook DB proof, hosted sandbox payment, receipt, refund, or restart proof here. Stripe test requires configured test credentials.                            |
| SHOP-05 subscriptions, entitlement, invoices            | partial     | `src/modules/commerce/subscription-service.ts`, `src/app/(frontend)/api/commerce/subscriptions/checkout/route.ts`, `src/app/(frontend)/api/commerce/subscriptions/route.ts`                                            | Service unit tests exist; member subscribe, change/cancel, invoice, access and dunning browser lifecycle unexecuted. Recurring provider sandbox required for hosted proof.                                                                  |
| SHOP-06 affiliate/referral and disclosure               | partial     | `src/modules/commerce/affiliate-service.ts`, `src/modules/commerce/referral-service.ts`, `src/app/(frontend)/api/commerce/affiliate/redirect/route.ts`                                                                 | Unit test exists; disclosed redirect, consent, conversion, reversal and payout ledger browser lifecycle unexecuted. No real payout authorized.                                                                                              |
| SHOP-07 donation intent, settlement and privacy         | **blocked** | `src/modules/commerce/DonationForm.tsx`, `src/app/(frontend)/api/commerce/donations/intent/route.ts`, `src/modules/commerce/donation-ingestion.ts`                                                                     | Definite response mismatch: form requires `actionUrl`, endpoint returns `intentId` and quote only. One-time and recurring donation cannot complete through that form. Hosted sandbox provider needed after checkout binding is implemented. |
| SHOP-08 Commerce Operations                             | partial     | `src/modules/admin/CommerceOperations.tsx`, `src/app/(frontend)/api/admin/commerce/dashboard/route.ts`                                                                                                                 | Unit boundary test passed; authorized drilldowns and reconciliation through browser unexecuted. Provider health depends on configuration.                                                                                                   |

## Nine-surface release inventory

| Surface      | Visible journey and source                                                                                        | Current proof classification                                        |
| ------------ | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| Publishing   | author, preview, publish (`src/modules/editorial`, `tests/browser/pub-06-renegadeparty-journey.spec.ts`)          | Prior browser proof; not rerun on this tree.                        |
| Presentation | template and visual builder (`src/modules/presentation`, `tests/browser/pre-03-visual-editor.spec.ts`)            | Prior browser proof; not rerun.                                     |
| Media        | upload, edit, version, reopen (`src/modules/media/image-editor`, `tests/browser/med-ext-01-image-editor.spec.ts`) | Partial: current isolated browser save/reopen persistence unproved. |
| Discovery    | search, feeds, redirects (`src/modules/public`, `tests/browser/disc-05-rendered-quality.spec.ts`)                 | Prior browser proof; not rerun.                                     |
| Workflow     | approval and timed release (`src/modules/editorial/workflow.ts`, `tests/browser/flow-06-command-center.spec.ts`)  | Prior browser proof; not rerun.                                     |
| Distribution | rendered outputs and provider handoff (`src/modules/public`, `docs/release/FEATURE_READINESS.md`)                 | Provider delivery requires configuration; not rerun.                |
| Audience     | consent and delivery (`src/modules/audience`, `tests/browser/aud-08-audience-pass-gate.spec.ts`)                  | Prior local proof; external delivery requires credentials.          |
| Community    | member/auth/moderation (`src/modules/community`, `tests/browser/comm-02-three-members.spec.ts`)                   | Prior browser proof; not rerun.                                     |
| Commerce     | buyer and operator journeys (SHOP paths above)                                                                    | Blocked by donation handoff and absent commerce browser acceptance. |

## Failures and exact next repair prompts

1. **Donation checkout binding — Commerce owner.** Reproduce: submit a valid `DonationForm`; the intent route responds 201 without `actionUrl`, and the form displays “Checkout did not return a secure payment page.” Repair prompt: “Bind a SHOP-07 donation intent to one authoritative SHOP-04 checkout session and server quote, including site, campaign version, recurrence, amount, currency, privacy, idempotency and expiry. Return only a provider-created hosted test URL after persistence. Exercise one-time and recurring sandbox donations, duplicate callbacks, receipts, refund/reversal and cross-site denial in DB integration and browser tests.” Depends on SHOP-04 hosted test provider setup.
2. **Commerce end-to-end gate — Commerce QA owner.** Reproduce: `rg -n 'checkout|refund|donat|affiliate|pod|subscription' tests/browser` returns no commerce journey tests. Repair prompt: “On a disposable migrated DB and isolated media/worker, add and run browser journeys for guest digital order and receipt, physical/POD paid order and tracking, member subscription access/change/cancel/invoice, affiliate consent/redirect/conversion/reversal, refund, and cross-site denial. Record exact IDs and persisted effects before and after restart. Use sandbox/emulator only.” Depends on prompt 1 and configured safe adapters.
3. **Payment concurrency and reconciliation — Commerce owner.** Repair prompt: “Run DB-backed concurrent coupon/inventory reservations, checkout replay, duplicate/out-of-order webhooks, unknown provider responses and worker restart/reconcile; assert one order, one grant, one POD submission intent, immutable purchased price, and append-only financial events. Preserve first failure and repair owning service.” Depends on isolated DB and provider emulator.
4. **Media editor persistence — Media owner.** Repair prompt: “Run isolated browser proof opening an existing image, editing it, saving a new version, verifying metadata/version persistence, and reopening the saved version; repair the first failing boundary.” Depends on isolated browser DB/media.
5. **Candidate and operations — Release owner.** Repair prompt: “Create an immutable candidate with a normal `npm ci`, production build outside a locked `.next`, clean guarded fresh/upgrade migrations, credential/config review, authenticated multi-site attacks, and media-aware backup/isolated restore with critical read/write checks. Record SHA and exact environment; rerun aggregate gates.” Depends on freezing the integration fixes.

Until these pass, `tested here` is limited to typecheck, lint and focused unit tests. Hosted payments, recurring billing and live POD remain `requires credentials/infrastructure`; the donation handoff is `not implemented` as an end-to-end path. The release decision is **BLOCKED**.

## Follow-up implementation on 2026-09-24

The original ledger above records the first failing boundary. Subsequent work in this checkout connected **one-time** donations to scoped carts, checkout sessions, payment intents, payment attempts, and a Stripe **test-mode** hosted URL. A durable unique donation idempotency key prevents a second checkout for the same submitted request; changed details under that key return 409. The public `/donate` list and `/donate/[campaignId]` page now render the form. The campaign API and checkout initiation reject cross-site campaign access. Custom amounts up to the campaign ceiling now agree with the form. Recurring checkout remains explicitly unavailable and is hidden on the public form until a real recurring mapping is implemented. No claim is made that a real payment was completed.

Evidence: `npm.cmd run typecheck` passed; focused donation route and ingestion suites passed (16 tests before the final cross-site case, then the route file passed 4/4); `npm.cmd run test:migrations:fresh` passed against newly created `shop08_donation_20260924_release_acceptance`, including `20260924_010000_donation_checkout_idempotency`; an isolated production `next build` using `RENEGADE_NEXT_DIST_DIR=.next-shop08-donation` passed before the public route was added. The final build and aggregate unit run are in progress at the time of this note and must be recorded separately. Unit mocks prove orchestration, not database or provider behavior. The worktree changed while this audit was in progress: HEAD became `dd1342aad05986a66bbdc5e2f682653d7f86efed`, so neither SHA identifies all current uncommitted bytes.

Remaining first boundaries: configured Stripe test credentials and a disposable migrated runtime for a browser payment/receipt; recurring donation plan/provider binding; a local or sandbox guest purchase, POD and affiliate lifecycle; authenticated operations drilldowns; media editor persistence; normal clean install and full restore. The release decision remains **BLOCKED**.

## Local one-time donation acceptance addendum

The local deterministic payment path is now executable through the public browser. It is explicitly gated by `LOCAL_E2E_TEST_MODE=true` and loopback host; the page says no real money is charged. The public `/donate` form creates a scoped, server-priced and idempotent donation checkout. `/checkout/test/[reference]` requires the guest checkout capability, and confirmation sends an HMAC-signed event through the normal webhook inbox. The commerce worker now polls the commerce queue. A corrective migration adds the missing commerce task enum values; donation ingestion now uses Payload-compatible `equals` queries. Receipt dispatch reads the guest cart email when there is no checkout proposal.

**Executed:** `tests/integration/shop-08-local-donation-checkout.integration.test.ts` passed on PostgreSQL after first exposing the job enum and query defects. The direct route, worker, replay, and DB assertions produced one donation, one order, one issued receipt, and one receipt delivery. The standalone browser script `scratch/shop08-browser-donation.mjs` passed `/donate` -> local hosted test page -> confirmed payment -> `/checkout/return` with a receipt. The browser run for session `33ce2e6b-c584-4285-a113-16d5121c8e93` was retained through web and commerce-worker restarts; the same browser context reloaded and still displayed `Payment confirmed`. SQL after restart returned `orders=1`, `donations=1`, `receipts=1`. The screenshot is `scratch/shop08-donation-proof.png`. These are local emulator claims only.

The first aggregate unit rerun passed 150 files and 997 tests. The final guarded fresh migration verifier passed on `shop08_final_20260924_release_acceptance`, including `20260924_010000_donation_checkout_idempotency` and `20260924_020000_commerce_job_enum`. An isolated production build using `RENEGADE_NEXT_DIST_DIR=.next-shop08-restart` passed and served the restart check. A prior aggregate integration run had 195/196 pass with a Discovery large-image card assertion; that single file then passed 9/9 on immediate isolated rerun. The final aggregate integration rerun is pending and must be recorded before treating that gate as passed. Lint and typecheck passed after a member settings link repair; format was being rerun after formatting the listed files.

The historical SHOP-07 row above is superseded **only for the local one-time donation flow**. Recurring donations, provider sandbox payment, broader guest/member checkout, affiliate/POD/refund browser journeys, multi-site attack suite, clean install, and complete restore remain unproved. The overall SHOP-08 and RELEASE-01 decision remains **BLOCKED**.

## Final gate update

The second complete integration run passed **36 files, 197 tests** on the migrated disposable PostgreSQL database. The isolated donation route suite passed **5/5** after the final change to return HTTP 409 if a concurrent request loses the unique idempotency-key race. Typecheck passed. The browser and restart check above used the tracked script `scripts/verify-shop08-local-donation.mjs`. The full aggregate unit run passed **150 files, 997 tests** before that last route change; the route's focused suite passed afterward. The isolated production build and guarded fresh migration passed as stated above. Lint and the final format check passed.

The first integration run's single Discovery card failure was not reproduced in the isolated file or complete second run. The local donation journey is proven within its emulator boundary; the remaining SHOP-08 journeys and release checks listed above remain open. **Release decision: BLOCKED.**
