# SHOP-08 Prompt 2 repository and installation proof — 2026-09-24

## Candidate and fixture provenance

Base HEAD `91d6108443ecd0dfecfb60ee997b8e7e6ee55d36`. The working tree already contained a modified upgrade test and an untracked beta proof before this pass; this report and documentation edits add more uncommitted bytes. The SHA therefore does not identify the exact tested tree. Windows PowerShell, Node 24.19.0, npm 11.17.0, PostgreSQL 17.6-alpine. All-module profile used `RENEGADE_MODULES=all` and `RENEGADE_ALLOW_UNSAFE_COLLECTION_COUNT=true`; integration and seed used `ALLOW_FIXTURE_SEED=true`. Runtime-only local test secrets were used; no provider connection was exercised.

The four databases were newly created, disposable, and isolated from the running site: `shop08_20260924_release_acceptance`, `shop08_20260924_upgrade_acceptance`, `shop08_20260924_integration_acceptance`, and `shop08_20260924_restore_acceptance`. The URL was derived from local `.env` by changing only the database name; no credential is recorded. Fresh and upgrade verifiers reset only the guarded acceptance databases. The fresh seed is the repository's neutral fixture. The upgrade fixture is generated by `verify-upgrade-migration.ts` from the `20260914_110000_med_05_video_workflow` baseline; it is synthetic, not an archived customer installation.

## Gates and triage

| Gate                                                | Result               | Counts / classification                                                                                                                                                                                                                                                                            |
| --------------------------------------------------- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm.cmd run format:check`                          | Pass                 | No skips reported.                                                                                                                                                                                                                                                                                 |
| `npm.cmd run lint`                                  | Pass                 | Zero warnings; no skips reported.                                                                                                                                                                                                                                                                  |
| `npm.cmd run typecheck`                             | Pass                 | No skips reported.                                                                                                                                                                                                                                                                                 |
| `npm.cmd run build`                                 | Pass                 | Production Next build completed.                                                                                                                                                                                                                                                                   |
| `npm.cmd test -- --reporter=dot`                    | Pass                 | 147 files, 983 passed, 0 failed or skipped.                                                                                                                                                                                                                                                        |
| `npm.cmd run test:integration -- --reporter=dot`    | Pass after migration | 35 files, 196 passed, 0 failed or skipped. First empty-database run: 27 failed files, 14 failed tests, 41 passed, 141 skipped; missing `sites`/`members` tables were an undocumented prerequisite, not a product assertion failure. `npm.cmd run db:migrate` then the unchanged full suite passed. |
| Three `shared-contract-*.integration.test.ts` files | Pass                 | 3 files, 82 passed, 0 failed or skipped, on the migrated integration database.                                                                                                                                                                                                                     |
| `npm.cmd run test:migrations:fresh`                 | Pass                 | 101 migration records, twice-applied verifier and physical Events column check. Initial attempt lacked `APP_URL` and `PAYLOAD_SECRET` process settings (configuration failure); rerun passed.                                                                                                      |
| `npm.cmd run test:migrations:upgrade`               | Pass                 | Synthetic supported baseline to current; 101 records, Events column and sentinel checks.                                                                                                                                                                                                           |

The integration log emits Next `revalidatePath` warnings because tests call publishing logic outside a Next request context. The suite passed; the warning was not suppressed and does not establish browser cache invalidation. `scratch/shop08-integration-after-migrate.log` and `scratch/shop08-shared-contract.log` hold local logs.

## Install, startup, upgrade, and restore

`git clone --local --no-hardlinks . C:\Projects\RENEGADE CMS\scratch-shop08-clone` followed by `npm.cmd ci --prefix <clone> --ignore-scripts` succeeded (884 packages). This proves lockfile resolution on a clean checkout; it does **not** prove a normal lifecycle-script install or build in that clone. npm reported 42 dependency audit findings; they were not triaged here.

The fresh acceptance database migrated and `npm.cmd run db:seed` succeeded; it contains two Events rows. The built standalone server on port 3334 returned HTTP 200 for `/health/live`, `/health/ready`, `/`, `/events`, `/store`, `/search`, and `/admin/commerce`. After stopping and restarting the process against the same database, readiness, Events, store, and commerce admin routes again returned 200. This is route availability, not authenticated workflow proof.

`pg_dump -Fc` of the seeded database was restored via `pg_restore` into the isolated restore database. It contained 101 migration records and two Events rows. A standalone server booted against the restored database on port 3336; readiness, Events, store, and commerce admin routes returned 200. No media tree was archived, no authenticated read/write was performed after restore, and no nine-surface history comparison was made. Operational restore acceptance is **PARTIAL**.

The upgrade verifier checks synthetic representative site/member/profile/media/content/job records and the Events repair, but it does not represent a previously deployed binary and does not compare every implemented community/commerce relationship, constraint, or index. A real prior-version image and fixture were not supplied. Supported production upgrade proof is **PARTIAL**; this blocks a release claim for customer upgrades.

## Reproduction and release consequence

On a new disposable database, set `DATABASE_URL`, `APP_URL`, `PAYLOAD_SECRET`, and the all-module flags above, then run `npm.cmd ci`, `npm.cmd run db:migrate`, `npm.cmd run db:seed` with `ALLOW_FIXTURE_SEED=true`, `npm.cmd run build`, and `npm.cmd run test:integration -- --reporter=dot`. Set guarded disposable `DATABASE_URL` and `UPGRADE_MIGRATION_DATABASE_URL` separately for the two migration verifiers. Run the standalone server with `PORT` and the same runtime settings. The exact fixture is the checked-in seed and verifier scripts, not an external dataset.

Repository gates and a seeded database boot pass. This evidence does not satisfy the complete Prompt 2 exit criterion because a normal clean-clone install/build, a real prior-version upgrade, full media-aware restore with authenticated critical writes, and exact candidate identity are unproved. The release remains **PARTIAL** and cannot be marked complete from this pass.
